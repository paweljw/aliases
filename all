alias pgdump="pg_dumpall >"
pgload() { psql -f $* postgres }

# ohmyzsh aliases
alias zshconfig="subl ~/.zshrc"
alias ohmyzsh="subl ~/.oh-my-zsh"

# bundler
alias be="bundle exec"
alias bi="bundle install"
alias bu="bundle update"
alias beg="be guard"
alias bec="be cap"
alias ber='RAILS_ENV=test be rspec'
alias bep='be pry'
alias bera='be rackup'
alias bepu='be puma'

# Ruby
alias rb='ruby'
alias brb='bundle exec ruby'

# Rails
alias rs='be rails server'
alias rc='be rails console'
alias ra='bundle exec rake'
alias rdf='ra db:drop db:create db:migrate'
alias rdfd='ra db:drop db:create db:migrate:with_data'
alias rdm='ra db:migrate'
alias rdr='ra db:rollback'
alias rat='ra -T'
alias rgm='be rails g migration'
alias rar='ra routes'
alias rds='ra db:seed'
alias rdfs='ra db:drop db:create db:migrate db:seed'

# Get off my lawn
alias cap="echo 'Use `bec` and get off my lawn'"

# ripgrep, heh
alias ag='/usr/local/bin/rg'

# Docker compose
alias dc='docker-compose'
alias dup='dc up'

# I mess this up way too much
alias dcup='dup'
alias dcd='dc down'

# Git aliases
alias gcmp='git diff --stat --color --graph'
alias gdrop='git reset --hard'
alias gbD='git branch -D'
alias nju='gco -b'

# Miscellanea
alias tf='tail -f'
alias tmux="TERM=screen-256color-bce tmux"
alias tx="tmux"
alias ups='ps aux | grep'
łups() { ps aux | grep $1 | grep -v grep | awk '{print $2}' | xargs kill }
łups!() { ps aux | grep $1 | grep -v grep | awk '{print $2}' | xargs kill -9 }
alias ip="curl ifconfig.me"
alias fu="fuck"

# Browser

function dgg() { open /Applications/Opera.app/ "https://duckduckgo.com/?q=$1"\ }
function gh() { open /Applications/Opera.app/ "https://duckduckgo.com/?q=!github $1"\ }
function gh.() { pwd | xargs basename | xargs -I{} open /Applications/Opera.app/ "https://duckduckgo.com/?q=!github {} "\ }
function rubygems() { open /Applications/Opera.app/ "https://duckduckgo.com/?q=!rubygems $1"\ }
function mdn() { open /Applications/Opera.app/ "https://duckduckgo.com/?q=!mdn $1"\ }

# NPM

alias ni="npm install"
alias ns="npm start"
alias nt="npm test"
alias nr="npm run"
alias nrg="nr grunt"
alias nk="nr karma"

# Oh, Vue
alias nrd="nr dev"

# Cargo

alias cr="cargo run"
alias cn="cargo new --bin"

# Foreman

alias fed="foreman start -f Procfile.dev"

# Aliases

alias are="source ~/.aliases/all"
alias apu='cd ~/.aliases && git add . && git commit -m "Added aliases @ `date`" && git push origin master && cd -'
alias aed='subl ~/.aliases/all'
alias au='cd ~/.aliases && git pull && cd -'

# Spotify

alias sp="spotify"
alias spp="sp pause"
alias spg="sp play"
alias spn="sp next"
alias sps="sp toggle shuffle"
alias spr="sp toggle repeat"
alias sptfu="sp vol 0"
alias spsh="sp share url"

# Sublime

alias stu="subl ."

# Directories

alias piw="cd ~/piwnica"
alias cod="cd ~/codest"

# Elixir

alias er='elixir'

source ~/.aliases/non_public
